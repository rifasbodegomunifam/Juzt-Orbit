{% extends "layouts/base.twig" %}
{% block content %}
    {% for item in order %}
      <section id="{{ item }}" data-section-id="{{ sections[item].section_id }}">
        {% include "sections/" ~ sections[item].section_id ~ ".twig" ignore missing with {'section': sections[item] } %}
      </section>
    {% endfor %}
{% endblock %}